apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  labels:
    chart: {{ .Values.appName }}
spec:
  type: {{ .Values.serviceType }}
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: helloworld
  selector:
    app: {{ .Values.appName }}
